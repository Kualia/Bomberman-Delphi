program Project;

uses
  Vcl.Forms,
  Main in '..\Main.pas' {MainForm},
  BaseClasses in '..\BaseClasses.pas',
  Game in '..\Game.pas',
  Helpers in '..\Helpers.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TMainForm, MainForm);
  Application.Run;
end.
